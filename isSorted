boolean isSorted(int[] array){
  for(int i = 0; i<array.length; i++){
    for(int j=i+1; j<array.length; j++){
      if(array[j] < array[i]){
        return false;
        }
    }
  }
  return true;
}

void main(){
  int[] testArray = {1, 2, 5, 4, 5};
  assert isSorted(testArray) == false;
}
